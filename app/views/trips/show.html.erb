    <h1> Vacation Name: <%= @trip.name %> </h1>

    <ul> 
    <li> Start Date: <%= @trip.start_date %> </li> 
    <li> End Date: <%= @trip.end_date %> </li> 
    </ul>
    
    <%= link_to 'Add Location', new_trip_location_path(@trip) %>

<hr>

<p> Update trip details </p>

<button class="btn btn-info"> <%= link_to 'Back', root_path%> </button>

<button class="btn btn-warning"> <%= link_to 'Update Details', edit_trip_path(@trip) %> </button>

<button class="btn btn-danger">  <%= link_to 'Delete Trip', @trip,  method: :delete %> </button>
